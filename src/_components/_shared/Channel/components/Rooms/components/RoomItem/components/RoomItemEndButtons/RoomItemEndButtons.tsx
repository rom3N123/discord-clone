import { ButtonGroup } from '@chakra-ui/react';
import React, { FC, ReactElement } from 'react';

const RoomItemEndButtons: FC = (): ReactElement => {
    return <ButtonGroup></ButtonGroup>;
};

export default RoomItemEndButtons;
